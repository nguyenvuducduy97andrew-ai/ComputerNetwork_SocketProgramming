# Cơ chế kiểm tra lỗi dữ liệu
def compute_internet_checksum(data: bytes) -> int:
    # Hàm tính Internet Checksum 16-bit

    return 0

def compute_file_hash(file_path: str, algorithm='sha256') -> str:
    # Hàm băm MD5/SHA-256 phục vụ lệnh HASH kiểm tra toàn vẹn
    
    return ''
